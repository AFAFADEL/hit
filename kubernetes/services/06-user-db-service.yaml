apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: sock-shop
  labels:
    app: user-db
spec:
  type: ClusterIP
  selector:
    app: user-db
  ports:
    - port: 27017
      targetPort: 27017

