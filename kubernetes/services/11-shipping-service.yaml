apiVersion: v1
kind: Service

metadata:
  name: shipping
  namespace: sock-shop

  labels:
    app: shipping

spec:
  type: ClusterIP

  selector:
    app: shipping

  ports:
    - port: 80
      targetPort: 80
