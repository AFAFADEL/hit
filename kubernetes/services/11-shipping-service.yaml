apiVersion: v1
kind: Service

metadata:
  name: shipping
  namespace: habiba-dev

  labels:
    app: shipping

spec:
  type: ClusterIP

  selector:
    app: shipping

  ports:
    - port: 80
      targetPort: 80
