apiVersion: v1
kind: Service
metadata:
  name: queue-master
  namespace: sock-shop
  labels:
    app: queue-master
spec:
  type: ClusterIP
  selector:
    app: queue-master
  ports:
    - port: 80
      targetPort: 80
