apiVersion: v1
kind: Service
metadata:
  name: carts-db
  namespace: habiba-dev
  labels:
    app: carts-db
spec:
  type: ClusterIP
  selector:
    app: carts-db
  ports:
    - port: 27017
      targetPort: 27017
