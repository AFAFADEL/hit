apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: catalogue-db-pvc
  namespace: sock-shop

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: ebs-gp3

  resources:
    requests:
      storage: 10Gi